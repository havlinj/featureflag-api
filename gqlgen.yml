schema:
  - graph/schema/*.graphqls   

exec:
  filename: graph/generated.go  

model:
  package: model
  filename: graph/model/models_gen.go 

resolver:
  package: graphql
  layout: follow-schema 
  dir: transport/graphql
  filename_template: "{name}.resolvers.go"